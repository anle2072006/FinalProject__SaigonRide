@model FinalProject__SaigonRide.Models.User
@{
    ViewData["Title"] = "Settings";
}

<div class="settings-wrapper">

    <div class="settings-col">
        <div class="st-card text-center profile-card">
            <img src="https://i.pravatar.cc/150?img=47" alt="Profile Avatar" class="profile-img" />
            <h3 class="profile-name">&#64;@(Model != null ? Model.Name : "User-Name")</h3>
            <p class="profile-email">@(Model != null ? Model.Email : "user@email.com")</p>
        </div>

        <div class="st-card info-card">
            <h4 class="st-card-title">Information</h4>
            <ul class="info-list">
                <li><strong>Name:</strong> <span>@(Model != null ? Model.Name : "Name, Last Name")</span></li>
                <li><strong>Email:</strong> <span>@(Model != null ? Model.Email : "user@email.com")</span></li>
                <li><strong>Tel:</strong> <span>@(Model != null && !string.IsNullOrEmpty(Model.PhoneNumber) ? Model.PhoneNumber : "+51 966 696 123")</span></li>
                <li><strong>Plan:</strong> <span>Hardcoded</span></li>
            </ul>

            <h4 class="st-card-title mt-4">Preferences</h4>
            <ul class="info-list">
                <li><strong>Plan:</strong> <span>Hardcoded</span></li>
                <li>
                    <strong>Light/dark:</strong>
                    <div class="toggle-switch">
                        <div class="toggle-circle"></div>
                    </div>
                </li>
            </ul>
        </div>
    </div>

    <div class="settings-col col-main">
        <div class="st-card h-100 form-card">
            <h3 class="st-section-title">User Settings</h3>
            <div class="form-section">
                <h4 class="st-card-title">Details</h4>
                <div class="form-grid">
                    <div class="form-group">
                        <label>Name</label>
                        <input type="text" value="@(Model != null ? Model.Name : "")" placeholder="Pepito Rodrick ..." />
                    </div>

                    <div class="form-group">
                        <label>Last Name</label>
                        <input type="text" placeholder="Coronel Sifuentes ..." />
                    </div>

                    <div class="form-group">
                        <label>Email</label>
                        <input type="email" value="@(Model != null ? Model.Email : "")" placeholder="pepito.c.sifuentes@uni.pe" />
                    </div>

                    <div class="form-group">
                        <label>Tel - Number:</label>
                        <div class="phone-input-group">
                            <input type="text" class="input-prefix" value="+51" readonly />
                            <input type="text" class="input-phone" value="@(Model != null ? Model.PhoneNumber : "")" placeholder="969 123 456" />
                        </div>
                    </div>
                </div>
                <button class="btn-neon">Save changes</button>
            </div>

            <div class="form-section mt-5">
                <h4 class="st-card-title">Password</h4>
                <div class="form-grid">
                    <div class="form-group">
                        <label>Change password</label>
                        <input type="password" placeholder="Put your password..." />
                    </div>

                    <div class="form-group">
                        <label class="invisible-label">Confirm password</label>
                        <input type="password" placeholder="Confirm password..." />
                    </div>

                    <div class="form-group">
                        <label>New password</label>
                        <input type="password" placeholder="Put your new password..." />
                    </div>

                    <div class="form-group">
                        <label class="invisible-label">Confirm new</label>
                        <input type="password" placeholder="Confirm new password..." />
                    </div>
                </div>

                <div class="form-actions">
                    <button class="btn-neon">Save changes</button>
                    <button class="btn-transparent">Forgot your password?</button>
                </div>
            </div>
        </div>
    </div>

    <div class="settings-col wallet-col">
        <div class="wallet-widget">
            <div class="wallet-header">
                <h2>My Wallet</h2>
                <span class="coin-badge">⭐ 2500</span>
            </div>

            <div class="wallet-inner-card">
                <div class="wallet-type">
                    <div class="icon-cash">$</div>
                    <div class="type-info">
                        <strong>Cash</strong>
                        <p>Default Payment Method</p>
                    </div>
                </div>

                <div class="wallet-balance-row">
                    <div class="balance-left">
                        <span>BALANCE</span>
                        <h2 class="text-red">$2500</h2>
                    </div>

                    <div class="balance-right">
                        <span>EXPIRES</span>
                        <h4>09/21</h4>
                    </div>
                </div>
            </div>
        </div>

        <div class="wallet-menu">
            <div class="menu-item">
                <span>Payment Methods</span>
                <span class="arrow">&gt;</span>
            </div>

            <div class="menu-item">
                <span>Coupon</span>
                <span class="menu-value">3 <span class="arrow">&gt;</span></span>
            </div>

            <div class="menu-item">
                <span>Integral Mall</span>
                <span class="menu-value">4500 <span class="arrow">&gt;</span></span>
            </div>
        </div>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/settings.css" asp-append-version="true" />
}